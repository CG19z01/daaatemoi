{
  "name": "daaatemoi",
  "version": "1.0.0",
  "description": "Site interactif de proposition de rendez-vous a Rouen",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env serveur/index.js",
    "dev": "node --watch --env-file-if-exists=.env serveur/index.js",
    "verifier": "node scripts-verification/verifier-syntaxe.js && node scripts-verification/verifier-lignes.js && node scripts-verification/verifier-pages.js && node scripts-verification/verifier-chargement.js",
    "test": "npm run verifier && node scripts-verification/verifier-experiences.js"
  },
  "engines": {
    "node": ">=20.18"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.2",
    "express": "^4.21.2"
  }
}
